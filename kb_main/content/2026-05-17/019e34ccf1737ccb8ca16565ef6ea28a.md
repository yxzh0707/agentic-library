---
uuid: 019e34cc-f173-7ccb-8ca1-6565ef6ea28a
node_type: raw
created_at: 2026-05-17T07:18:26.931Z
updated_at: 2026-05-17T07:20:34.384Z
created_by: human:default
created_by_run: import:markdown:meta.md
l0_summary: 为时间特征添加上下文门控，提升序列时间信号在推荐模型中的有效性。
l1_overview: 在I-625基础实验上，对原始24维时间特征添加门控（Linear(ctx_dim→24)+sigmoid），在序列嵌入投影前应用，上下文来自平均物品token和用户密集特征。门控使用零初始化残差缩放，确保起始行为与基线一致。实验通过AUC阈值（≥0.824推进）评估门控能否让模型动态强调时间/间隔维度。如果有效，可考虑堆叠其他特征或领域特定启用；如果无效，可能时间投影已足够，转而尝试其他正则化方法。
embeddings:
  e_l0_id: null
  e_l1_id: null
  e_l2_id: null
  model: null
  embedded_at: null
wikilinks: []
current_path: null
derived_state:
  cluster_id: null
  cluster_membership_strength: null
  is_cluster_hub: false
  hub_of_cluster: null
lifecycle:
  status: active
  reference_count: 0
  last_accessed_at: null
  superseded_by: null
  superseded_reason: null
---

# I-639_time_feat_gating

```yaml
id: I-639
name: time_feat_gating
status: ready_for_smoke
base: experiments/I-625_gap_features/code/
fork_base: "experiments/I-625_gap_features/code/"
prior:
  expected_lb_auc: 0.824
  range: [0.821, 0.826]
  p_improve_over_baseline: 0.38
  baseline_lb_auc: 0.822863
amp_compile_caveat: "Inherited from I-625 base (amp+compile on in run.sh; compile skipped on non-CUDA smoke)."
rationale:
  - I-625 gap features validated the 24-dim dense seq_time_feats path as the active time-signal carrier.
  - Codex challenge verdict required pre-projection gating on raw 24-dim time features, not post-projection d_model tokens.
  - Context-conditioned modulation can let the model emphasize time/recency/gap dimensions differently by target item and user dense state.
  - Residual zero-init alpha preserves identity at initialization and makes flag-off behavior identical to I-625.
changes:
  - Add TimeFeatGate: Linear(ctx_dim -> 24), sigmoid gate, residual scale `1 + alpha * (gate - 0.5)`.
  - Gate applies before `seq_time_proj[domain](seq_time_feats)` inside `_embed_seq_domain`.
  - Per-domain ModuleDict gates; each gate owns a zero-init learnable scalar alpha.
  - Context is concat(mean item NS token embedding, projected user dense token).
  - New CLI flag: --use_time_feat_gating (default 0; run.sh sets 1).
decision_thresholds:
  promote_if: "leaderboard_auc >= 0.824"
  neutral: "0.821 <= leaderboard_auc < 0.824"
  kill_if: "leaderboard_auc < 0.821"
next_actions:
  promote:
    - Stack with I-635 item heat if both improve independently
    - Consider domain-specific enablement if one sequence domain dominates gains
  neutral:
    - Check learned alpha magnitudes by domain before deciding stackability
    - Test target-only context if user dense context appears noisy
  kill:
    - Time feature projection may already learn sufficient context downstream
    - Pivot to I-640 token dropout or I-638 sparse regularization
```

## Notes

The I-625 forward path exposes `item_ns` and `user_dense_tok` before sequence
embedding, so no tensor-name fallback was needed. If user dense features are
absent in a future schema, the implementation falls back to target item context
alone.

## Outcome

(To be filled after evaluation)

